web:
  build: ./docker/app-node
  command: sh weboo-webapp/target/weboo-webapp.war
  ports:
   - "8080"
  volumes:
   - .:/code
lb:
  build: ./docker/load-balancer
  command: /start-haproxy
  ports:
   - "8080:8080"
  links:
   - web
#redis:
#  image: orchardup/redis
  